[project]
name = "cookbook"
version = "0.1.0"
description = "A command-line cookbook for managing and searching recipes by ingredients."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "simple-term-menu>=1.6.6",
]
